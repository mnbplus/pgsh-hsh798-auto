interface:
  display_name: "PGSH Automation"
  short_description: "Onboard PGSH accounts and run cooldown-aware daily automation."
  default_prompt: "Use $pgsh-project-automation to manage PGSH automation in this repository, including safe daily runs and cooldown decisions."

policy:
  allow_implicit_invocation: true
