interface:
  display_name: "PGSH Automation"
  short_description: "Run cooldown-aware PGSH account automation in this repo."
  default_prompt: "Use $pgsh-project-automation to run the PGSH daily workflow in this repository and explain what to do next."

policy:
  allow_implicit_invocation: true
